server:
  port: 8086

spring:
  application:
    name: spring-webflux-nosql
  data:
    mongodb:
      database: ${DATABASE_NAME}
      uri: ${DATABASE_URI}
      # database: AngomaTours
      # uri:  mongodb+srv://ciropoma_db:yZ2uIezkd2qR4KOX@cluster0.dhuboqy.mongodb.net/?retryWrites=true&w=majority
springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html1